#include <funshield.h>
int ledky[4] = {led1_pin, led2_pin, led3_pin, led4_pin};
int butony[3] = {button1_pin, button2_pin, button3_pin};
constexpr int ledky_count = sizeof(ledky) / sizeof(ledky[0]);
constexpr int butony_count = sizeof(butony) / sizeof(butony[0]);int konstanta = 0;bool nesviti = true;bool stisknuto1;bool stisknuto2;// the setup function runs once when you press reset or power the boardvoid setup() {for(int i = 0; i < ledky_count; i++){pinMode(butony[i], INPUT);}for(int i = 0; i < ledky_count; i ++){pinMode(ledky[i], OUTPUT);}stisknuto1 = digitalRead(button1_pin);stisknuto2 = digitalRead(button1_pin);}void encoder(unsigned int v){for(int i = ledky_count; i > 0; i--){bool write = (v & 1 == 1);digitalWrite(ledky[i], write);}else{digitalWrite(ledky[i], HIGH);}v = v>>1;}}// the loop function runs over and over again forevervoid loop() {if(stisknuto1 != digitalRead(button1_pin)){konstanta = (konstanta + 1)%16;encoder(konstanta);}if(stisknuto2 != digitalRead(button2_pin)){konstanta = (konstanta +15)%16;encoder(konstanta);}delay(120);}